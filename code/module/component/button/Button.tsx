import React, {type ReactNode} from "react";

function Button(): ReactNode {
    return (
        <button {...{
            style: {
                all: "unset",
                backgroundC
            }
        }}>
        </button>
    );
}