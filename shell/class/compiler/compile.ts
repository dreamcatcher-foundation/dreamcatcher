import { CompilerMiddleware } from "./interface/CompilerMiddleware.ts";


function compile(middleware: CompilerMiddleware, payload) {
    return middleware(payload);
}


const compiler = (function() {
    let self;

    function 


    return function() {self ? self : (self = {

    });}
})();