import type {ReactNode} from "react";
import {FlexRow} from "@component/FlexRow";

export function PulseLine(): ReactNode {
    return <>
        <FlexRow
        style={{
            width: ""
        }}>

        </FlexRow>
    </>;
}